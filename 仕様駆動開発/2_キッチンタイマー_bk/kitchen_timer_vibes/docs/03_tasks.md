# 実装手順書（tasks.md）

## 1. ディレクトリと空ファイルの準備
- [ ] `kitchen_timer_vibes/` フォルダを作成する
- [ ] `docs/` フォルダ内のファイルを確認する
- [ ] `kitchen_timer_vibes/` 直下に `index.html`, `style.css`, `script.js` の空ファイルを作成する

## 2. UI（HTML/CSS）の実装と基本配置
- [ ] `index.html` の骨組み（タイマー表示、入力エリア、ボタン）をマークアップする
- [ ] `style.css` で各要素をレイアウトし、タイマーが見やすいようにスタイルを適用する

## 3. ロジック（JavaScript）の基本実装
- [ ] `script.js` に変数を定義し、残り時間を管理する仕組みを作る
- [ ] タイマーのカウントダウンロジック（setInterval）を実装する
- [ ] 一時停止（clearInterval）とリセット（clearInterval＋値のクリア）のロジックを実装する

## 4. 画面とロジックの結合（ボタン連携・結果表示）
- [ ] HTMLのボタン要素にイベントリスナー（click）を設定する
- [ ] 「スタート」ボタン押下でタイマーが開始されるようにする
- [ ] 「ストップ」ボタン押下でカウントが止まるようにする
- [ ] 「リセット」ボタン押下でタイマーが0に戻るようにする
- [ ] 0分00秒になったら「タイムアップ！」と画面に表示されるようにする

## 5. 動作確認（手動テスト）
- [ ] 任意の時間を設定し、「スタート」ボタンでカウントダウンが始まること
- [ ] 「ストップ」ボタンでカウントダウンが止まること
- [ ] 再度「スタート」で止めた時間から再開すること
- [ ] 「リセット」ボタンでタイマーが0:00に戻ること
- [ ] 時間経過後、正しく「タイムアップ！」と表示されること
